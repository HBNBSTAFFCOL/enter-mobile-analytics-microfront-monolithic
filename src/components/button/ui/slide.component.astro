<ul class="slide__cards" id="slide__cards_list">
    <slot />
</ul>

<style>
    .slide__cards {
        height: auto;
        width: 100%;
        margin: auto;
        white-space: nowrap;
        overflow-x: auto;
        overflow-y: hidden;
        scroll-snap-type: x mandatory;
        display: flex;
        list-style: none;
        padding: 0;
        margin: 0;
        gap: 1rem;
        scrollbar-width: thin;
        scroll-behavior: smooth;
        scrollbar-color: var(--color-action--log) rgba(0, 0, 0, 0);
        scrollbar-gutter:stable both-edges;
    }

    .slide__cards > * {
        /* display: inline-block;
      scroll-snap-align: center;
      margin-right: 10px;
      width: 100%;
      */
    }
</style>
